<!DOCTYPE html>
<html lang="pt-BR">
	<head>
		<meta charset="UTF-8" />
		<meta name="viewport" content="width=device-width, initial-scale=1.0" />
		<title>Relatório DISC + VAC</title>
		<link rel="preconnect" href="https://fonts.googleapis.com">
		<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
		<link href="https://fonts.googleapis.com/css2?family=Manrope:wght@300;400;500;600;700&display=swap" rel="stylesheet">
		<style>
			:root {
				--font-family: 'Manrope', -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, Helvetica, Arial, sans-serif;
				--color-primary: #286bfe;
				--color-text: #333333;
				--color-text-light: #666666;
				--color-background: #ffffff;
				--color-surface: #f7f9fc;
				--border-radius: 8px;
			}

			body {
				font-family: var(--font-family);
				color: var(--color-text);
				background-color: var(--color-background);
				line-height: 1.6;
				margin: 0;
				padding: 0;
				-webkit-print-color-adjust: exact;
			}

			.report-container {
				max-width: 800px;
				margin: 0 auto;
				padding: 20px;
			}

			.header {
				display: flex;
				justify-content: space-between;
				align-items: center;
				border-bottom: 2px solid var(--color-primary);
				padding-bottom: 20px;
				margin-bottom: 30px;
			}

			.header .logo {
				max-height: 60px; /* Ajusta a altura máxima da logo */
			}

			.header .report-title {
				text-align: right;
			}

			.header h1 {
				margin: 0;
				font-size: 24px;
				font-weight: 600;
			}
			.header p {
				margin: 0;
				font-size: 14px;
				color: var(--color-text-light);
			}

			.section {
				margin-bottom: 40px;
			}

			.section-title {
				font-size: 20px;
				font-weight: 600;
				color: var(--color-primary);
				border-bottom: 1px solid #e0e0e0;
				padding-bottom: 8px;
				margin-bottom: 20px;
			}

			.participant-details {
				background-color: var(--color-surface);
				border-radius: var(--border-radius);
				padding: 20px;
				display: grid;
				grid-template-columns: 1fr 1fr;
				gap: 10px 20px;
			}
			.participant-details div {
				font-size: 14px;
			}
			.participant-details strong {
				color: var(--color-text);
				display: block;
				margin-bottom: 2px;
				font-weight: 600;
			}
			.participant-details span {
				color: var(--color-text-light);
			}

			.results-grid {
				display: grid;
				grid-template-columns: 1fr;
				gap: 30px;
			}

			.result-card {
				background-color: var(--color-surface);
				border-radius: var(--border-radius);
				padding: 20px;
			}

			.result-card h3 {
				font-size: 18px;
				margin-top: 0;
				margin-bottom: 15px;
			}

			.result-card .dominant-profile {
				font-size: 16px;
				margin-bottom: 25px;
			}
			.result-card .dominant-profile span {
				font-weight: 700;
				color: var(--color-primary);
			}

			.chart-container {
				text-align: center;
				margin-bottom: 25px;
			}

			.description p {
				margin: 0 0 10px;
				font-size: 14px;
				white-space: pre-wrap; /* Mantém as quebras de linha do texto original */
			}

			.footer {
				text-align: center;
				margin-top: 50px;
				padding-top: 20px;
				border-top: 1px solid #e0e0e0;
				font-size: 12px;
				color: #999999;
			}
		</style>
	</head>
	<body>
		<div class="report-container">
			<header class="header">
				<img
					src="https://jump.net.br/wp-content/uploads/2024/04/Copia-de-Design-sem-nome-2-3-768x273.png"
					alt="Logo JUMP"
					class="logo"
				/>
				<div class="report-title">
					<h1>Relatório Comportamental</h1>
					<p>DISC + VAC</p>
				</div>
			</header>

			<section class="section">
				<h2 class="section-title">Dados do Participante</h2>
				<div class="participant-details">
					<div>
						<strong>Nome:</strong>
						<span>{{participant.name}}</span>
					</div>
					<div>
						<strong>E-mail:</strong>
						<span>{{participant.email}}</span>
					</div>
					<div>
						<strong>Data de Geração:</strong>
						<span>{{generationDate}}</span>
					</div>
				</div>
			</section>

			<section class="section">
				<h2 class="section-title">Análise de Perfil DISC</h2>
				<div class="result-card">
					<p class="dominant-profile">Seu perfil dominante é: <span>{{discResult.dominant}}</span></p>
					<div class="chart-container">
						{{barChart discResult.data maxValue=discResult.maxValue colors='["#FF6B6B", "#4ECDC4", "#45B7D1", "#F9C74F"]'}}
					</div>
					<div class="description">
						<p>{{discResult.description}}</p>
					</div>
				</div>
			</section>

			<section class="section">
				<h2 class="section-title">Análise de Perfil VAC</h2>
				<div class="result-card">
					<p class="dominant-profile">Seu perfil dominante é: <span>{{vacResult.dominant}}</span></p>
					<div class="chart-container">
						{{barChart vacResult.data maxValue=vacResult.maxValue colors='["#5E81AC", "#81A1C1", "#88C0D0"]'}}
					</div>
					<div class="description">
						<p>{{vacResult.description}}</p>
					</div>
				</div>
			</section>

			<footer class="footer">
				<p>Este é um relatório confidencial gerado pela JUMP.</p>
				<p>&copy; {{generationDate}} Todos os direitos reservados.</p>
			</footer>
		</div>
	</body>
</html>
